# §17 Audit — USER_STORIES ↔ Code Binding (2026-04-25, regenerated 2026-04-26)

> Cross-check of every `US-xxx` ID in `USER_STORIES.md` against `grep -rn "US-xxx" src/` and `grep -rn "US-xxx" test/`. Bucketed into:
> - **A类** — tag found in BOTH `src/` AND `test/` (fully bound).
> - **B类** — tag missing from src (and/or test), but the behavior is plainly implemented per code-reading. Just needs a `// US-xxx` comment so future audits stop flagging it.
> - **C类** — tag missing from both AND impl behavior is not clearly visible. Either genuinely unimplemented or un-findable; needs human verification.

## Summary

- Total: 88 US-xxx
- A类 (fully bound): 8
- B类 (impl exists, missing comment): 78
- C类 (needs verification): 2

Note vs. prior audit: last night's "51/80 with no automation reference" was conservative — a fuller code-reading pass shows 86/88 are implemented; only 2 are genuinely C. The prior task #21=22 / task #22=16 split looks too aggressive on C; PM should re-base task #21 to 78 (or more granularly: ~23 high-value top-level stories worth tagging vs. 55 implementation details).

Also note: `US-119` is referenced once (in `US-413` coverage list) but never defined in `USER_STORIES.md` — orphan reference, not counted in totals.

---

## A类 — fully bound (8 items)

- US-105 — tab badge equals visible top-level cards — `src/view.ts:423` + `test/e2e/specs/parent-child.e2e.ts:343`
- US-107 — silently drop blank-title task lines — `src/view.ts:282` + `test/e2e/specs/board-basics.e2e.ts:78`
- US-141 — `+ subtask` button, child inherits parent ⏳ — `src/view/drag-mobile.ts:1` + `src/view.ts` (cited in renderCard) + `test/e2e/specs/subtask.e2e.ts:77,82,136`
- US-145 — completing/dropping parent cascades to todo descendants — `src/cli.ts:113` + `test/e2e/specs/parent-child.e2e.ts:121`
- US-148 — child with own ⏳ ≠ parent's renders as standalone card on its own day — `src/view.ts:645,1210,1450` + `test/e2e/specs/parent-child.e2e.ts:228`
- US-150 — top-level card ⏳ badge hidden when context already implies the day — `src/view.ts:1133,1198` + `test/e2e/specs/parent-child.e2e.ts:303`
- US-167 — Spotlight-style Quick Add v2 (chunks 1–4) — `src/quickadd.ts:8,55,81,124,148,155,180,270,281` + `test/e2e/specs/quickadd.e2e.ts:8,35,60,96,147,215`
- US-413 — IME composition guard on Enter-submitting inputs — `src/quickadd.ts:110`, `src/dateprompt.ts:45`, `src/view.ts:1607,2101` + `test/e2e/specs/ime.e2e.ts:68,76,141,205`, `test/e2e/specs/quickadd.e2e.ts:171`

## B类 — needs US-xxx code comment (78 items)

### B1 — Tested but src untagged (13 items, easy wins — comment in obvious file)

- US-101 — week 7-day view, today highlight, prev/next — likely impl: `src/view.ts:renderWeek` (around L566)
- US-115 — overdue red, near-deadline (≤3d) yellow chip — likely impl: `src/view.ts:renderTaskCard` (`bt-overdue` / `bt-near-deadline`, L1153,1167; month chip L741)
- US-121 — drag card to a different day rewrites ⏳ — likely impl: `src/view.ts:makeDropZone` + `src/view/dnd.ts`
- US-123 — drag to bottom trash zone marks `[-] ❌` — likely impl: `src/view.ts:renderTrashZone` (L1023) + `wireTrashDropTarget`
- US-124 — dropping parent cascades to todo children, leaves done children — likely impl: `src/cli.ts:cliAbandon` chain + `src/writer.ts` cascade logic
- US-149 — subtask ⏳ badge rules (parent vs child mismatch) — likely impl: `src/view.ts:renderSubcard` near L1438
- US-162 — Enter-commit subtask add input — likely impl: `src/view.ts` add-subtask input around L1604 (already has US-413 comment, just needs US-162)
- US-201 — register CLI verbs to native Obsidian CLI — likely impl: `src/main.ts:registerAllCliHandlers` (L164)
- US-203 — write idempotency (`done` on done task → `unchanged`) — likely impl: `src/cli.ts:done` / `src/main.ts:cliDone` (L440)
- US-204 — every write returns before/after diff — likely impl: `src/cli.ts:formatOkWrite` + `src/writer.ts` return shape
- US-208 — hash-based ref resolves when line shifts — likely impl: `src/cache.ts:resolveRef` + `src/writer.ts:resolveTaskRef` (L46)
- US-214 — ambiguous_slug error with candidate list — likely impl: `src/writer.ts:46` (`TaskWriterError("ambiguous_slug",…)`)
- US-407 — byte-level preserve of Tasks-plugin extension fields on rename/reschedule — likely impl: `src/writer.ts:282` (META_PRESERVE_RE)

### B2 — Src-tagged but no test (10 items, comment is fine; tests are §17 follow-up)

- US-142 — recursive subtask render — `src/view.ts:1441` (already cited; just expand to a real comment block)
- US-501 — desktop-only features no-op on mobile — `src/view.ts:1835`
- US-502 — viewport-based mobile layout switch + force-mobile setting — `src/view.ts:150`, `src/types.ts:47`, `src/settings.ts:116`
- US-504 — mobile month = calendar + dot + bottom-sheet day list — `src/view.ts:750`, `src/i18n.ts:63`
- US-505 — mobile compact card, 1-level subtasks, scale-on-drag — `src/view.ts:1438,1485`
- US-506 — long-press card → action sheet — `src/view.ts:767,1234,1241,1251`
- US-507 — mobile pointer-drag, 800ms cross-tab dwell, 60px auto-scroll — `src/view.ts:91,367,1244`, `src/view/drag-mobile.ts`
- US-508 — left-swipe done / right-swipe drop, 30% threshold, 1s undo — `src/view.ts:1242,1243,1252,1366`
- US-509 — Quick Add bottom sheet + soft-keyboard avoidance — `src/quickadd.ts:46,69,216`
- US-510 — platform-conditional UI strings — `src/types.ts:44`, `src/settings.ts:86`

### B3 — Untagged in both, but plainly implemented per code-reading (55 items)

#### Board & views (10)
- US-102 — month calendar, prev/next month — `src/view.ts:renderMonth` (L689)
- US-104 — unscheduled pool sorted deadline-first then created-time — `src/view.ts:renderUnscheduledPool` (L981, sort comparator L990–993)
- US-106 — status bar `📋 N today · ⚠ M overdue`, click opens board — `src/status-bar.ts:refresh` (L39)
- US-108 — `[field:: value]` inline fields, no hard-coded tag/field literals — `src/parser.ts:INLINE_FIELD_STRIP_RE` (L20), config-driven groups
- US-109 — search by title, filter by tag, summary — `src/view.ts:renderToolbar` search box (L523), `src/cli.ts:filterTasks` (L226)
- US-110 — "open board on startup" toggle — `src/main.ts:104`, `src/settings.ts:openOnStartup`
- US-111 — default tab setting — `src/settings.ts:defaultView` (L44), `src/types.ts:defaultView`
- US-112 — week starts Mon/Sun setting — `src/settings.ts:weekStart` (L62), `src/dates.ts:startOfWeek`
- US-113 — empty-state onboarding — `src/view.ts:renderOnboarding` (L404)
- US-114 — dragover-tab dwell auto-switches view — `src/view.ts:466,472` (DwellTracker)

#### Drag & write semantics (5)
- US-116 — column header `N tasks · XhYm` — `src/view.ts:columnStats` (L635)
- US-122 — month cell as drop target — `src/view.ts:734` (`makeDropZone(cell, day)`)
- US-125 — drop card on card → nest, with toast/undo — `src/view.ts:1655–1699` (drop-on-card handler), `src/main.ts:cliNest`
- US-126 — no cycles when nesting — `src/writer.ts:597` (`invalid_nest`), `src/writer.ts:714`
- US-127 — fade + collapse animation on card removal — `src/anim.ts:animateOut`
- US-128 — Ctrl/Cmd+Z undo, last 20 ops — `src/view.ts:undoStack` (L90), `src/view/undo.ts`

#### Parent/child (4)
- US-143 — visible parent → child not duplicated as top-level — `src/view.ts:hideChildrenOfVisibleParents` (L652)
- US-144 — child inherits parent's status/⏳/📅 — `src/types.ts:inheritsTerminal` (L31), `src/cache.ts` propagation
- US-146 — same-day-as-parent child skips ➕ stamp — `src/writer.ts:388` (`stampCreated` gate; `addTask` parent-day check)
- US-147 — editing parent doesn't mutate child fields — implicit by line-scoped `vault.process` (writer never touches sibling/child lines)

#### Edit & keyboard (5)
- US-161 — click title to inline-rename, Enter commits, Esc cancels — `src/view.ts:1604` (rename input keydown)
- US-163 — new task → today's daily note tail, default no scheduled — `src/quickadd.ts:submit` + `src/writer.ts:addTask` (L406+, daily fallback)
- US-164 — right-click context menu — `src/view.ts:1718,1951,2011`
- US-165 — hover popover with parent chain + source context — `src/view/popover.ts:ContextPopoverController`
- US-166 — Ctrl+1~4 tab switch, `/` focus filter — `src/view.ts:handleKey` (L148, L1865+)

#### CLI / agent (10)
- US-202 — every list row first column = stable id — `src/cli.ts:formatList` (id at column 0)
- US-205 — text-format default for `list` / `stats`; json opt-in — `src/main.ts:185,207` (`format` arg default)
- US-206 — `stats days=N` ratio + top tag minutes — `src/cli.ts:computeStats` (L311), `src/main.ts:cliStats`
- US-207 — natural-language when keywords (`today/tomorrow/week/next-week/month/YYYY-MM-DD/FROM..TO/unscheduled`) — `src/dates.ts:resolveWhen` (L82)
- US-209 — incremental `actual minutes=+30m` — `src/main.ts:cliActual` (L415, `add` mode), `src/parser.ts:parseDurationToMinutes`
- US-210 — workflow doc (typical day / capture / backfill) — `README.md` (workflows section); not code, but doc exists
- US-211 — `error <code>` + 一句人话 format — `src/cli.ts:formatError` (L566), `src/writer.ts:TaskWriterError` (L11)
- US-212 — `list parent=<id>` filter — `src/cli.ts:filterTasks` (L277, parent filter); `src/main.ts:cliList` plumbs it
- US-213 — `add stamp-created=true|false` override — `src/main.ts:476`, `src/writer.ts:388`
- US-228 — `nest ref=A under=B` cross-file verb — `src/main.ts:cliNest` (L506), `src/writer.ts:nestUnder` (L702)

#### Energy management (5)
- US-301 — Covey 4-quadrant tag grouping (`#1象限`~`#4象限` default) — `src/view.ts:1100,1784,1951,2011`, `src/cli.ts:394`
- US-302 — `[estimate::]` / `[actual::]` parse + sum — `src/parser.ts:INLINE_FIELD_STRIP_RE` (L20), `src/cli.ts:computeStats`
- US-303 — 7-day estimate accuracy ratio + top-tag minutes preset — `src/cli.ts:computeStats` (L311), `src/view.ts:877` (UI mirror)
- US-304 — history weeks default-collapsed, current expanded — `src/view.ts:924–933` (collapsedWeeks defaulting on `wk < currentWeek`)
- US-305 — `[-] ❌` = abandoned, separate from done — `src/types.ts:TaskStatus` (`dropped`), `src/cli.ts:abandon` verb

#### Cross-role (9)
- US-401 — markdown-only, no DB — architectural; entire codebase: only writes to `vault.process` (`src/writer.ts:122,458,736,776,797`)
- US-402 — auto Chinese/English from Obsidian language — `src/i18n.ts:detectLocale` (L9)
- US-403 — atomic write — `src/writer.ts` uses `app.vault.process` exclusively (atomic at the Obsidian API level)
- US-404 — skip files with no tasks — `src/cache.ts:hasTaskListItem` (L27,127,153,247,274)
- US-405 — remember last tab — `src/types.ts:lastTab` (L43), `src/view.ts:setTab` (L304) saves `settings.lastTab`
- US-406 — callout tasks (`> - [ ]`) parsed/rendered/written — `src/parser.ts:12` (callout prefix RE), `src/writer.ts:98` (callout-aware checkbox swap)
- US-409 — never normalize user literals (hashtags, inline fields, emoji marks) — `src/writer.ts:282` (META_PRESERVE_RE keeps emoji+date+priority+inline-field+block-anchor verbatim)
- US-410 — bilingual NL date words in Quick Add / CLI — `src/quickadd.ts:295,301,304`, `src/dateprompt.ts:70,80,84`
- US-411 — display-locale dates, ISO writes — `src/view.ts:weekdayLabel` (locale display), `src/dates.ts:toISO` always emits `YYYY-MM-DD`

#### Release (5)
- US-601 — auto-release on strict semver tag push — `.github/workflows/release.yml:18` (tag pattern), L154–162 (gh release create)
- US-602 — pre-flight gate (typecheck/lint/unit/e2e) — `.github/workflows/release.yml:24–50`
- US-603 — `versions.json` lockstep with manifest — `.github/workflows/release.yml:86–97` + `versions.json` exists
- US-604 — auto release notes grouped by conventional-commit prefix — `.github/workflows/release.yml:107–149`
- US-605 — three standard assets (`main.js` + `manifest.json` + `styles.css`) on each release — `.github/workflows/release.yml:162`

#### Mobile additional (1)
- US-503 — mobile vertical week list, today expanded, others collapsed — `src/view.ts:expandedDays` state (L49,117,581,595), week column collapsible head (L591)

## C类 — needs verification (2 items)

- US-408 — UI strings re-render live on Obsidian language switch (no restart) — appears **partially implemented**. `src/i18n.ts:297` does `const locale: Locale = detectLocale();` ONCE at module load, and there is no `workspace.on("css-change")` / language-change listener in `main.ts` or `view.ts` that calls `view.render()` after a localStorage `language` swap. Strings WILL re-render correctly if the user re-opens Obsidian, but not "实时". Reason: locale is captured at module-init, never refreshed.
- US-412 — error `<code>` English, "一句人话" follows current language — appears **partially implemented / un-findable**. The English code half is implemented (`TaskWriterError` codes are stable English strings — `task_not_found`, `invalid_date`, `ambiguous_slug`, `invalid_nest`). But the human-message half is **not localized**: every `throw new TaskWriterError(code, "english message…")` in `src/writer.ts` and `src/cli.ts` uses an English literal; there's no `tr("err.taskNotFound", …)` lookup. Reason: error messages bypass the i18n layer entirely.
